# Railway automatically injects DATABASE_URL and REDIS_URL when you add those services.
# You only need to set these manually:

JWT_SECRET=change_this_to_a_long_random_secret
PORT=3001

# Railway provides these automatically — DO NOT set them manually if using Railway add-ons:
# DATABASE_URL=postgresql://...
# REDIS_URL=redis://...
